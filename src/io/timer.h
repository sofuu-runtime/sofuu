/*
 * timer.h — setTimeout, setInterval, clearTimeout, clearInterval, sofuu.sleep
 */
#ifndef SOFUU_TIMER_H
#define SOFUU_TIMER_H

#include "quickjs.h"
#include <stdio.h>

void mod_timer_register(JSContext *ctx);

#endif
